

local classes = {}

local function
--
--
local function createInstance(class, ...)
  local instance = {}
end

--
--
local function createClass(name)
  local attributes = {
    name = name or "?"
  }
  local methods = {}
  local static = {}
  local class = setmetatable({}, {
    __index =
    __newindex = addIndex
  })

end
